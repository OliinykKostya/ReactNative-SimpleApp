import React from "react"
import { View,Text} from 'react-native'

const ErrorMessage = () => (
  <View style={{ flex: 1, justifyContent: "center", }}>
    <Text style={{ textAlign: "center" }}>Reload later</Text>
  </View>
)

export default ErrorMessage
